  language: python
  before_install:
    - sudo apt-get -y chromium chromium-chromedriver
  install:
    - pip install -r requirements-dev.txt
  addons:
    apt:
      update: true
  script:
    - pytest